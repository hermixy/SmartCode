#include <QCoreApplication>
#include <control.h>
#include <TranslationUnit.h>
#include <DiagnosticClient.h>
#include <QFile>

namespace boo
{

Computer user;

Computer  submit()
{
int cv_shoot;
return cv_shoot;
}

class abdo
{
public:
enum Case{Int,Float};
private:
enum IconType{PublicIcon,PrivateIcon};
int max = 45;
int min;
class hirachy
{
int zlib;
int rex;
T a;
List<int> m_list;
};
};

template<class T> T abs(T x,T y)
{
return x+y;
}


int main(int argc, char *argv[])
{
  for(;;)
  {
  int user;
  if(user)
  {
  Control max;
  cout << "dd";
  break;
  }
  }
  int max_sz;
  
  Control link;
  const QByteArray localFileName = fileName.toUtf8();
  _control->setDiagnosticClient(0);

    QString fileName = "main.cpp";
  TranslationUnit *_translationUnit;


  const StringLiteral *fileId = _control->stringLiteral(localFileName.constData(),
                                                        localFileName.size());
  _translationUnit = new TranslationUnit(_control, fileId);

  QFile f(fileName);
  f.readAll();

  _translationUnit->setLanguageFeatures(LanguageFeatures::defaultFeatures());
  (void) _control->switchTranslationUnit(_translationUnit);

  return 0;
}
}

